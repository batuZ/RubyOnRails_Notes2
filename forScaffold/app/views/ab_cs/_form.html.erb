<%= form_with(model: abc, local: true) do |form| %>
  <% if abc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(abc.errors.count, "error") %> prohibited this abc from being saved:</h2>

      <ul>
      <% abc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
